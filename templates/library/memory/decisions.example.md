# Decisions — memory (example)

> This is a seed example so you can see the shape. Delete it once your own
> distilled files exist. The `/cuz-distill` pass writes files like this from your
> capture inbox — one topic per file, durable facts/decisions as tight bullets.

## Example
- **Pick boring defaults for infra.** Decided to stay on Postgres + a single box
  until there's a measured reason to add anything; revisit only on real load.
  [[example-skill]]

---
name: example-skill
description: A seed example showing the shape of a distilled cuz skill. Replace or delete it. Use when you want to see how a repeated procedure becomes an auto-loadable SKILL.md.
---

# Example skill

This is a placeholder so a fresh brain isn't empty. The `/cuz-distill` pass
promotes a procedure into a skill like this when it's shown up more than once.

Durable facts live in `library/memory/`; this is a *procedure* — the steps to do
a recurring thing the same way every time. Delete this once you have real ones.

## Steps
1. Do the first thing.
2. Do the second thing.
3. Verify it worked before calling it done.
